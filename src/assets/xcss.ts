import { xcss } from "@atlaskit/primitives";

export const commonCompWrap = xcss({
  display: "flex",
  flexFlow: "column",
  gap: "space.400",
});
export const commonInputWrap = xcss({
  display: "flex",
  flexFlow: "column",
  width: "33%",
});
export const pageContentWrap = xcss({
  margin: "0 auto",
  padding: "space.800",
  
});
